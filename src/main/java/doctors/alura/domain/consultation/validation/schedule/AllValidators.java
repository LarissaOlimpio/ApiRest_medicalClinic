package doctors.alura.domain.consultation.validation.schedule;

import doctors.alura.domain.consultation.ConsultationData;

public interface AllValidators {
    void validator(ConsultationData data);
}
